由printf函数C语言基础数据类型相关知识。
printf是C语言的标准库函数，用于格式控制字符串的输出。格式控制字符串输出的最重要的是数据的格式。
printf的格式控制字符串组组成格式如下：
    %[flags][width][.prec][length]type
    示意：
    %[标识][最小宽度][精度][类型长度]类型

printf函数的实例代码。

在C程序设计语言中有一个问题问\c会输出什么，编译提示未知的转义序列。